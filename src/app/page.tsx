export default function HomePage() {
  return (
    <main style={{ maxWidth: 960, margin: "0 auto", padding: "var(--space-12) var(--space-4)" }}>
      <h1 className="title-xl">Landed</h1>
      <p className="text-secondary" style={{ marginTop: "var(--space-2)" }}>
        Phase 0 scaffold. Career data entry arrives in the next steps.
      </p>
    </main>
  );
}
