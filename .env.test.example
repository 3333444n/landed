# Copy to .env.test. Integration and browser tests use this database and truncate it.
DATABASE_URL=postgres://landed:landed@localhost:5432/landed_test
# Tests never call a model provider: the fake adapter answers from examples/generation.
LANDED_MODEL_PROVIDER=fake
LANDED_ARTIFACT_DIR=./artifacts-test
